# Agentic Coding

## Model Context Protocol (MCP) Servers

- For Zed Editor, configured in `.zed/settings.json` > `context_servers`.
- `CLAUDE.md` sym linked to `AGENTS.md`, commited to repo.
- `CLAUDE.local.md` for custom configuration, not commited to repo.
- Tidewave: http://localhost:4000/tidewave
- Conductor for parellel agents: https://conductor.build/
